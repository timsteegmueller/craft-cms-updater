Projekt: Automatisierung der Updates unserer Craft CMS Systeme

🥅 Ziel
•	Verständnis für CI/CD-Prozesse im Webentwicklungsumfeld
•	Praktische Erfahrung mit N8N zur Automatisierung technischer Prozesse
•	Umgang mit Composer, Craft CMS, GitHub API, Envoyer Deployments und automatisierten Backups
•	Sicherheit im Umgang mit Fehlerfällen, Monitoring und einfachen Tests
•	Erkennen, wie Deployment-Workflows in einem professionellen Setup orchestriert werden
🛠️ Aufgabe
Automatisiere das Update aller Craft CMS Instanzen, die wir hosten. Das erfolgt über folgende Schritte:
1.	Überwachung: In definierten Zyklen (z. B. wöchentlich oder monatlich) prüft N8N, ob Updates verfügbar sind.
2.	Update-Aktion:
o	Der Befehl php craft update all muss ausgeführt werden
o	Technisch steckt ein composer update dahinter
3.	Integration mit GitHub:
o	Die aktualisierte composer.lock und ggf. andere Dateien müssen als Pull Request ins zugehörige GitHub-Repo eingecheckt werden
4.	Backup:
o	Vor jedem Update muss ein Backup der Datenbank erstellt werden
5.	Einfacher Test nach dem Update:
o	Ein HTTP-Request auf die Startseite des Projekts soll HTTP 200 OK zurückgeben
o	Fehlerhafte Antworten (500er) müssen erkannt und gemeldet werden
 

✅ Definition of Done
•	N8N läuft automatisch und regelmäßig
•	Für jedes Projekt wird automatisch ein PR mit den aktualisierten Abhängigkeiten erstellt
•	Vor jedem Update wird ein Datenbank-Backup erstellt
•	Nach jedem Update erfolgt ein automatischer Funktionstest (HTTP 200 auf /)
•	Fehlerhafte Deployments (500er Fehler) werden in einem Log oder Alert dokumentiert
•	Das System ist modular, sodass neue Craft-Projekte leicht eingebunden werden können
•	Es ist dokumentiert, wie man Projekte hinzufügt oder die Automatisierung manuell anstößt


 
Spickzettel
Craft CMS Update lokal:
php craft update all (Quelle: https://craftcms.com/docs/commerce/5.x/update.html)
GitHub PR via API:
•	Branch erstellen
•	Änderungen commiten (z. B. neue composer.lock)
•	Push auf Repo
•	PR über GitHub API öffnen
Deployment:
•	Wir deployen über Laravel Envoyer
•	GitHub Push → Envoyer übernimmt Live-Rollout
Backup erstellen (je nach Setup):
•	z. B. via mysqldump:
mysqldump -u USER -p DATABASE > backup.sql
HTTP-Test nach Deployment:
•	Mit N8N Node "HTTP Request":
o	Methode: GET
o	URL: https://deinprojekt.de/
o	Erwarteter Status: 200
Challenge bei der PHP-Ausführung:
•	Da es sich um ein GitHub-Repo handelt, muss der PHP-Befehl entweder:
o	per SSH remote auf dem Dev-Server ausgeführt werden (über N8N und ein SSH Node)
o	oder in einer GitHub Action / CI-Umgebung abgebildet werden, falls dort eine Umgebung bereitsteht
o	oder …

